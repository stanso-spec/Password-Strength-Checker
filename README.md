# Password-Strength-Checker
A simple Python application that checks password strength by validating length, uppercase letters, numbers, and special characters to determine whether a password is strong or weak
